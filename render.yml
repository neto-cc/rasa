env:
  - name: PYTHON_VERSION
    value: "3.10.10"

services:
  - type: web
    name: rasa-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    port: 10000  # Renderの指定ポート

